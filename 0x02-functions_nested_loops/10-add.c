#include "main.h"

/**
 * add - takes two integer arguments and adds them together.
 * @a: first integer
 * @b: second integer
 *
 * Return: the result;
 */
int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
